{
  "name": "mie359-study-site",
  "private": true,
  "version": "1.0.0",
  "description": "Static MIE359 study site for Cloudflare Pages.",
  "scripts": {
    "build:data": "python3 build.py",
    "build:static": "python3 export_static.py",
    "build": "python3 build.py && python3 export_static.py",
    "deploy:pages": "python3 build.py && python3 export_static.py && npx wrangler pages deploy dist --project-name mie359-study-site",
    "deploy:assets": "python3 build.py && python3 export_static.py && npx wrangler deploy",
    "serve": "python3 -m http.server 8000"
  }
}
